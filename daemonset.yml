apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: mydaemon
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: mypod
        image: nginx:latest
        ports:
        - containerPort: 80
